[project]
name = "report_core_dev"
version = "0.11.0"
description = "report-core-root"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "maturin>=1.9.0",
    "ruff>=0.11.13",
]

[tool.uv.workspace]
members = [
    "core_pyo3",
]
